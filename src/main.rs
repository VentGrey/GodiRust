use std::io;

//Constante para limitar el número de entradas
const MAX:i64 = 50;

struct cliente {
    ID: String,
    nombre: String,
    telefono: String,
    dia: u64,
    mes: u64,
    anio: u64,
} cliente[MAX];

fn main() {

}
